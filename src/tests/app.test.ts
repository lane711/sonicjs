import { describe, it, expect } from 'vitest'

describe('SonicJS AI', () => {
  it('should be able to run tests', () => {
    expect(true).toBe(true)
  })

  it('should have correct project name', () => {
    const projectName = 'SonicJS AI'
    expect(projectName).toBe('SonicJS AI')
  })
})